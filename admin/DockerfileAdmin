FROM node:10.15.3

 
ENV user admin_backend
 
RUN useradd -m -d /home/admin ${user} \
 && chown -R ${user} /home/admin/
 
USER ${user}

WORKDIR /home/admin/app
COPY app ./
COPY package.json ./


EXPOSE 80

CMD npm config set cache-min 9999999 \
    && npm ic \
    && npm run start:dev